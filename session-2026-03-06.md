# Claw Mini-PC Session Log — March 5–6, 2026

## Summary

Completed stability hardening, SSH security, and brought OpenClaw fully online with Telegram bot integration.

## Completed

### Stability

- **Static IP configured** — Set `192.168.86.38/24` via netplan on `wlp1s0` (WiFi). Replaced DHCP config in `/etc/netplan/90-NM-f24f686b-8707-42b6-9d58-98691377b4fa.yaml`. Gateway: `192.168.86.1`, DNS: `192.168.86.1` + `8.8.8.8` fallback.
- **Sleep/suspend disabled** — Masked `sleep.target`, `suspend.target`, `hibernate.target`, `hybrid-sleep.target` via systemctl.

### Security

- **SSH hardened** — Disabled password authentication (`PasswordAuthentication no`) and root login (`PermitRootLogin no`) in `/etc/ssh/sshd_config`. Restarted `ssh.service`. Verified key-based login still works.
- **SSH key backed up** — Private key (`C:\Users\chris\.ssh\id_ed25519`) saved to password manager.

### OpenClaw

- **Auto-start confirmed** — `openclaw-gateway.service` is a user-level systemd service, enabled and linked in `default.target.wants`. Starts on boot automatically.
- **Dedicated user confirmed** — `openclaw` user already exists at `/home/openclaw` with lingering enabled (`/var/lib/systemd/linger/openclaw`).
- **Anthropic API key added** — Ran `openclaw config` wizard, selected Model section, entered API key. Model set to `anthropic/claude-opus-4-6`.
- **Telegram bot fixed** — Bot token had a typo (letter `O` vs digit `0` in `AAG5E0QGf`). Corrected via `openclaw config set channels.telegram.botToken`. Bot `@MaresiasWaveBot` ("Higgens") is live and responding.

## Current State

| Component | Status |
|---|---|
| IP | Static `192.168.86.38` |
| SSH | Key-only, no root, port 22 |
| Firewall (UFW) | Active, SSH only |
| Fail2ban | Installed |
| Auto-updates | Enabled |
| Sleep/suspend | Disabled |
| OpenClaw gateway | Running, auto-start on boot |
| Dashboard | `http://localhost:18789` via SSH tunnel |
| Model | `anthropic/claude-opus-4-6` |
| Telegram | `@MaresiasWaveBot` connected, pairing mode |

## Remaining TODO

- [ ] BIOS: enable auto power-on after AC loss (requires physical access + reboot)
- [ ] Set up router DHCP reservation for `192.168.86.38` as belt-and-suspenders
- [ ] Explore OpenClaw dashboard features (sessions, cron jobs, skills)
- [ ] Customize agent persona via `SOUL.md` and `AGENTS.md`
- [ ] Test heartbeat functionality
- [ ] Security audit: `openclaw security audit --deep`

## Git Commands

```powershell
cd C:\Tools\openclaw-config
copy session-2026-03-06.md .
git add session-2026-03-06.md
git commit -m "Session log: stability hardening + Telegram bot live"
git push
```
